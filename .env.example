# PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ipobaje"

# Redis
REDIS_HOST="localhost"
REDIS_PORT="6379"

# Credential encryption — REQUIRED — generate: openssl rand -hex 32
ENCRYPTION_KEY=""

# NextAuth — REQUIRED — any random string
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# App
NODE_ENV="development"
